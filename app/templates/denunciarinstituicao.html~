{% extends 'baseusuario.html' %}

{% block denunciarins %}

<div class="col-md-6 col-md-offset-2">
		<h2 style="color: brown">Denunciar Instituição</h2>
		<form action="{{url_for('denunciarinstituicao')}}" method="POST">
		
		{{ form.csrf_token }}
		<div class="form-group">
			<label style="color: brown">ID da instituição:</label>
			{{form.id_i(class="form-control")}}	
			{%if form.errors.id_i%}
			<div class="alert alert-danger" role="alert">
			<ul>
			{% for e in form.errors.id_i %}
				<li>{{e}}</li>
			{% endfor %}
			</div>
			{%endif%}
		</ul>
		</div><div class="form-group">
			<label style="color: brown">Descrição:</label>
			{{form.descricao(class="form-control")}}	
			{%if form.errors.descricao%}
			<div class="alert alert-danger" role="alert">
			<ul>
			{% for e in form.errors.descricao %}
				<li>{{e}}</li>
			{% endfor %}
			</div>
			{%endif%}
		</ul>
		</div>
		<div class="form-group">
			<label style="color: brown">Data:</label>
			{{form.data(class="form-control")}}	
			{%if form.errors.data%}
			<div class="alert alert-danger" role="alert">
			<ul>
			{% for e in form.errors.data %}
				<li>{{e}}</li>
			{% endfor %}
			</div>
			{%endif%}
		</ul>
		<p></p>
		<div>
			<button type="submit" class="btn btn-default"  style="background-color: #FFDEAD; color: brown">Informar</button>
		</div>
		</form>
		
	<h1>  </h1>
        </div>

{% endblock %}
